<UserControl x:Class="PhotoBooth.Controls.LivePreviewControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="1200">
    <Border Name="PreviewBorder"
            CornerRadius="0"
            Background="#0a0506"
            ClipToBounds="False"
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            MaxWidth="1200"
            MaxHeight="600">
        <Border.BorderBrush>
            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                <GradientStop Color="#AAFFFFFF" Offset="0"/>
                <GradientStop Color="#88FFFFFF" Offset="0.5"/>
                <GradientStop Color="#AAFFFFFF" Offset="1"/>
            </LinearGradientBrush>
        </Border.BorderBrush>
        <Border.BorderThickness>
            <Thickness>3</Thickness>
        </Border.BorderThickness>
        <Border.Effect>
            <DropShadowEffect Color="Black" BlurRadius="40" ShadowDepth="0" Opacity="0.6"/>
        </Border.Effect>
        <!-- Inner glassy border -->
        <Border CornerRadius="0"
                BorderBrush="#66FFFFFF"
                BorderThickness="1"
                Margin="3"
                ClipToBounds="True">
            <Border.Effect>
                <DropShadowEffect Color="#FFFFFF" BlurRadius="8" ShadowDepth="0" Opacity="0.4"/>
            </Border.Effect>
        
            <Grid>
                <!-- Live Camera Feed -->
                <Image Name="PreviewImage" 
                       Stretch="Uniform"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center"/>
            </Grid>
        </Border>
    </Border>
</UserControl>



